# ============================================================
# Dockerfile.agent — Sara AI Observability Container (Flow Mode)
# ============================================================

FROM grafana/agent:latest

# Copy Flow config
COPY agent-config.river /etc/agent/config.river

EXPOSE 12345

ENTRYPOINT ["/usr/bin/grafana-agent"]
CMD ["run", "--config.file=/etc/agent/config.river"]
