# ============================================================
# Dockerfile.agent — Sara AI Observability Container (Final)
# ============================================================

FROM grafana/agent:latest

# Copy our custom config
COPY agent-config.yaml /etc/agent/config.yaml

EXPOSE 12345

ENTRYPOINT ["/usr/bin/grafana-agent"]
CMD ["run", "--config.file=/etc/agent/config.yaml"]
