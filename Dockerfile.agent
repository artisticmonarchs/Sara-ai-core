# ================================================================
# Dockerfile.agent — Grafana Agent for Render (Flow Mode)
# ================================================================

# Use official Grafana Agent
FROM grafana/agent:v0.32.0

# Copy River config into container
COPY agent-config.river /etc/agent/config.river

# Expose Render port
ENV PORT=12345

# Run Grafana Agent with explicit config file
CMD ["/usr/bin/grafana-agent", "run", "--config.file=/etc/agent/config.river"]
