remote_write {
  url = env("GRAFANA_CLOUD_REMOTE_WRITE_URL")
  basic_auth {
    username = env("GRAFANA_CLOUD_INSTANCE")
    password = env("GRAFANA_CLOUD_API_TOKEN")
  }
}

logs {
  clients {
    url = env("GRAFANA_CLOUD_LOKI_URL")
    basic_auth {
      username = env("GRAFANA_CLOUD_INSTANCE")
      password = env("GRAFANA_CLOUD_API_TOKEN")
    }
  }
}

# Optional: adjust/remove node_exporter target if not running inside container
# metrics {
#   wal_directory = "/tmp/wal"
#   global {
#     scrape_interval = "15s"
#   }
#   configs {
#     scrape_configs {
#       job_name = "node"
#       static_configs {
#         targets = ["localhost:9100"]
#       }
#     }
#   }
# }
