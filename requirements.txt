Flask==3.0.3
Werkzeug==3.0.3
Jinja2==3.1.4
itsdangerous==2.2.0

# WSGI server + WebSocket worker (matches your start command logs)
gunicorn==22.0.0
gevent==24.2.1
gevent-websocket==0.10.1
greenlet==3.0.3

# Flask WebSocket support
flask-sock==0.7.0
simple-websocket==1.0.0

# Background jobs + Redis
celery==5.3.6
redis==5.0.1

# Telephony
twilio==9.2.3

# HTTP, env, time utils
requests==2.32.3
python-dotenv==1.0.1

# OpenAI client used by gpt_client.py
openai>=1.13.0,<2.0.0

# Metrics endpoint used by app.py
prometheus-client>=0.20.0,<1.0.0

# Error reporting used by redis_client.py / celery paths
sentry-sdk>=1.39.0,<2.0.0

# Cloudflare R2 (S3-compatible)
boto3>=1.34.0,<2.0.0
